[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

# React Router needs all paths to fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
